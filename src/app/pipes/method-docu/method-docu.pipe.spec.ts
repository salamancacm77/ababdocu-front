import { MethodDocuPipe } from './method-docu.pipe';

describe('MethodDocuPipe', () => {
  it('create an instance', () => {
    const pipe = new MethodDocuPipe();
    expect(pipe).toBeTruthy();
  });
});
